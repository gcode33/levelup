# Supabase — copy these from your Supabase project dashboard (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# LLM provider (server-side only — never exposed to the client)
LLM_API_KEY=
# LLM_BASE_URL=https://api.deepseek.com   # optional, for non-OpenAI providers
# LLM_MODEL=deepseek-chat                  # optional
